{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "RelaGrid (Tauri dev)",
      "type": "node-terminal",
      "request": "launch",
      "command": "npm run dev",
      "cwd": "${workspaceFolder}",
    },
    {
      "name": "RelaGrid (build)",
      "type": "node-terminal",
      "request": "launch",
      "command": "npm run build",
      "cwd": "${workspaceFolder}",
    }
  ]
}
